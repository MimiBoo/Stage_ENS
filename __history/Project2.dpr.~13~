program Project2;

uses
  Vcl.Forms,
  Unit2 in 'Unit2.pas' {Form2},
  Vcl.Themes,
  Vcl.Styles,
  Data in 'Data.pas' {dbData: TDataModule},
  point in 'point.pas' {Form1},
  Settings in 'Settings.pas' {Form3},
  AddAdmin in 'AddAdmin.pas' {Form4},
  Login in 'Login.pas' {Form5},
  add_Stage in 'add_Stage.pas' {Form6};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm5, Form5);
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TdbData, dbData);
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm3, Form3);
  Application.CreateForm(TForm4, Form4);
  Application.CreateForm(TForm6, Form6);
  Application.Run;
end.
